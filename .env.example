# OCI Provider configuration (sourced automatically by devShell)
# Copy to .env and fill in your values
# Find at: OCI Console -> Profile (top right) -> Tenancy / User Settings / API Keys

TF_VAR_oci_tenancy_ocid="ocid1.tenancy.oc1..aaaa..."
TF_VAR_oci_user_ocid="ocid1.user.oc1..aaaa..."
TF_VAR_oci_fingerprint="xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx"
TF_VAR_oci_region="us-ashburn-1"

# OCI API private key path (must be absolute, ~ won't expand)
# Generate with: openssl genrsa -out ~/.oci/oci_api_key.pem 2048
# Upload public key to OCI Console -> Profile -> User Settings -> API Keys
TF_VAR_oci_private_key_path="/home/YOUR_USER/.oci/oci_api_key.pem"

# SSH public key for instance access
# Get with: cat ~/.ssh/id_ed25519.pub
TF_VAR_ssh_public_key="ssh-ed25519 AAAA..."
